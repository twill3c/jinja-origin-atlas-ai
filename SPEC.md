# SPEC.md — jinja-origin-atlas-ai(Jinja Origin Atlas AI)

<!-- scaffold template v1.30.0 から展開(2026-09-08)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

日本全国の神社を公開情報だけから地図に置き、系統・祭神・創建年代・地理条件を突き合わせて
探索できるようにする。あわせて、権利条件を機械的に検査したテキストにのみ AI の意味解析を
掛け、**史実・伝承・AI 推定を画面の上で混ぜない**。

課金経路をひとつも持たない(外部 DB・推論サーバ・有料 API を使わない)。
AI の結果はすべて事前計算し、静的アーティファクトとして配る。

## 2. 実測したデータ源(2026-09-08)

**この節の数値はすべて実測である。**母集団とクエリを併記する。再実測の手順は §11。

| ID | 源 | 測ったもの | 実測値 | 権利 |
|---|---|---|---|---|
| M-01 | OSM Overpass | 日本国内 `amenity=place_of_worship`+`religion=shinto` | **40,776**(node 21,673 / way 19,012 / relation 91) | ODbL |
| M-02 | OSM Overpass | 日本国内 `building=shrine` | **5,280**(node 57 / way 5,191 / relation 32) | ODbL |
| M-03 | Wikidata | `P31/P279* Q845945`(神社)全世界 | **31,011** | CC0 |
| M-04 | Wikidata | 同・座標 P625 あり | **17,073** | CC0 |
| M-05 | Wikidata | 同・日本(P17=Q17)かつ座標あり | **16,917** | CC0 |
| M-06 | Wikidata | 日本の神社(P17=Q17)総数 | **29,838** | CC0 |
| M-07 | Wikidata | 祭神 **P825「献呈先」** を持つ | **11,888** | CC0 |
| M-08 | Wikidata | 社格 **P13723** を持つ | **7,936** | CC0 |
| M-09 | Wikidata | 成立日 P571 を持つ | **1,117** | CC0 |
| M-10 | Wikidata | 母院 P612 を持つ(**文献上の分祀関係**) | **1,093** | CC0 |
| M-11 | Wikidata | OSM ID を直接持つ(P10689 way / P11693 node / P402 relation) | **945**(way 720 / node 209 / relation 16、うち座標あり 943) | CC0 |
| M-12 | Wikidata | ja.wikipedia 記事を持つ | **6,145** | 記事本文は CC BY-SA |
| M-13 | Wikidata | ja 説明文(schema:description)を持つ | **26,478**(400 件標本で中央値 **13 字**・最大 35 字・50 字以上 **0**) | CC0 |
| M-14 | ジャパンサーチ | `keyword=神社&keyword=由緒` の hit | **1,892** | レコードごと |
| M-15 | ジャパンサーチ | 同・先頭 100 件の内訳 | `description` 欄あり **22**/100、0 字 **78**/100、100 字以上 **13**/100、`contentsRightsType` 欄そのものが無い **33**/100、権利 ALLOW(cc0/pdm/ccby)**39**/100、**ALLOW かつ 100 字以上 = 5**/100 | — |
| M-16 | 国土地理院 | `dem_png`(DEM10B, z14)で諏訪湖湖面を復号 | **759.0 m**(公表標高 759 m と完全一致) | PDL1.0 基本 |
| M-17 | 国土地理院 | `dem5a_png` は z15 で配信・`dem_png` は z14 で配信 | z15 の `dem_png` は **HTTP 404** | 同上 |
| M-18 | 国土交通省 国土数値情報 | 利用約款 / W05 / N03 / C23 の各ページ | いずれも **HTTP 200**。`robots.txt` は無し(404 が HTML で返る) | PDL1.0 基本・個別確認 |

### 2.1 この実測が仕様書 V1.0 に対して意味したこと

- **祭神は P1049 ではなく P825 である。** P1049(worshipped by)で測ると **1 件**しか無い。
  仕様書は「dedicated to / enshrined deity 相当」と幅を持たせていた。実データのプロパティ
  頻度を測って確定させた
- **社格 P13723(7,936 件)は仕様書に無い資産だった。** 式内小社 4,598 / 国幣社 4,236 /
  村社 1,907 / 郷社 948 / 官幣社 736 / 県社 727 / 名神大社 369 / 別表神社 350 / 一宮 107 ほか
- **母院 P612(1,093 件)は §43 の「実線 = 公開史料で確認できる関係」の実体である。**
  AI 類似度(点線)と混ぜない
- **Wikidata→OSM の直リンク 945 組は、名寄せの非循環オラクルになる**(§7)
- **仕様書 §12 の AI の柱は、仕様書どおりの rights_gate では立たない。** M-15 を M-14 に
  外挿すると AI 可能な由緒本文は **約 95 件**(見込み。1,892 × 5/100。本番 ETL で実測に
  置き直す)。HDBSCAN の `min_cluster_size=15` も UMAP も成立しない
- **Wikidata の ja 説明文は AI コーパスにならない**(M-13)。中央値 13 字で
  「東京都千代田区の神社」の形。埋め込んでもクラスタは都道府県名を測るだけになる

## 3. 決定 D-01 — AI コーパスの舵(2026-09-08・人間の判断)

**採用: 構造化データ + ja.wikipedia の併用。**

| 用途 | 出所 | 件数(実測) | 権利の扱い |
|---|---|---|---|
| 系統分類(§9) | Wikidata P825 祭神 / P13723 社格 / 名称規則 | 11,888 / 7,936 | CC0。そのまま再配布 |
| 文献上の分祀関係(§43 実線) | Wikidata P612 母院 / P361 | 1,093 | CC0。そのまま再配布 |
| 由緒の意味解析(§12) | ja.wikipedia の該当記事 | 6,145 | CC BY-SA。**本文は再配布しない**。埋め込み由来のスコアのみ配布し、帰属とライセンスを明示する |
| 由緒本文の閲覧導線 | ジャパンサーチ | 1,892(ALLOW 由緒本文は約 95 の見込み) | rights_gate を通したものだけ |

**仕様書 §5.4 からの逸脱を明記する。** 仕様書は `ccbysa` を「再配布ライセンス単純化のため
AI 本文コーパスから既定除外、必要なら V1.1 で個別設計」としていた。D-01 はこれを V1.0 で
解除する。解除の条件は次の三つで、`LICENSE-DATA.md` に同じ文言を置く。

- CC BY-SA 本文を**公開アーティファクトに含めない**(埋め込みベクトルも含めない)
- 配るのは**数値スコア**(motif score / similarity / cluster id / UMAP 座標)だけ
- 各神社詳細に、由来した記事の**帰属とライセンスと版**を表示する

RULE-01(公開されている ≠ AI 学習可能)は解除しない。ja.wikipedia を使えるのは
**ライセンスが明示され、帰属条件を満たせるから**であって、公開されているからではない。

## 4. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 全国の神社位置レイヤーを公開データから生成し地図に描く | must |
| F-02 | OSM 出典表示(`© OpenStreetMap contributors`)を地図上に常時表示する | must |
| F-03 | Wikidata の構造化属性(祭神・社格・成立日・別名・読み仮名)を神社に結合する | must |
| F-04 | 名寄せ(OSM × Wikidata)を距離と名称類似度で行い、confidence を残す | must |
| F-05 | `rights_gate` を通ったテキストだけを AI 本文として扱う | must |
| F-06 | 標高を国土地理院 DEM から取得する | must |
| F-07 | 最寄り河川距離を国土数値情報 W05 から計算する | must |
| F-08 | 系統分類を「構造化 → 祭神 → 名称 → 由緒 → AI 推定」の優先順位で決め、根拠を表示する | must |
| F-09 | 由緒テキストの Embedding・類似神社 Top20・12 モチーフ・HDBSCAN・UMAP を事前計算する | must |
| F-10 | 伝承年代と史料確認年代を分離して保持・表示する | must |
| F-11 | 属性単位で出典を表示する | must |
| F-12 | 画面: `/` `/map` `/shrine/?id=` `/similar/?id=` `/ai-space` `/analytics` `/sources` `/about-ai`(D-06 で詳細と類似をクライアント描画に変更) | must |
| F-13 | 全 AI 画面に免責(§46)を表示する | must |
| F-14 | 文献上の分祀関係(実線)と AI 意味類似(点線)を視覚的に分離する | must |
| F-15 | 海岸距離は V1.0 では `null`(C23 の非商用条項のため。仕様書 §5.6 の既定 3) | should |

## 5. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 有料 API・DB サーバ・GPU サーバ・Vercel 上の推論をひとつも使わない | 依存とコードの走査(G-05) |
| N-02 | 公開 JSON は 1 ファイル 5 MB(gzip)を超えない。超えたら都道府県分割 | ビルド時に実測しレポート(G-06) |
| N-03 | 初期 JS < 400 KB gzip・初期地図データ < 3 MB gzip(いずれも目標) | ビルドレポートで実測 |
| N-04 | Python は `>=3.11,<3.14`。この機の system python は 3.14 のみのため uv で 3.12 を立てる | `uv sync` の成否 |
| N-05 | 外部取得は失敗しても既存の公開データを消さない | 部分ビルドのテスト |
| N-06 | WCAG 2.2 AA 相当(キーボード操作・ラベル・コントラスト・色以外の手掛かり) | 実ブラウザ検品 |

## 6. 品質ゲート

| ID | ゲート | 落とす条件 |
|---|---|---|
| G-01 | JSON Schema | 公開アーティファクトの schema 違反が 1 件でもあれば落とす |
| G-02 | 権利未確定の混入 | AI コーパスに ALLOW 以外・またはコード欠落のレコードが 1 件でもあれば落とす |
| G-03 | 神社 ID の一意性 | 重複が 1 件でもあれば落とす |
| G-04 | 座標の妥当性 | 日本の外接矩形外・NaN が 1 件でもあれば落とす |
| G-05 | 課金経路ゼロ | 有料 API のホスト名・SDK が依存または出荷コードに現れたら落とす |
| G-06 | 公開 JSON 容量 | 単一公開 JSON が gzip 5 MB を超えたら落とす |
| G-07 | 出典の欠落 | 由緒本文を持つのに `source_ids` が空のレコードがあれば落とす |
| G-08 | モデル版の記録 | AI レコードに model revision が無ければ落とす |
| G-09 | 名寄せの非循環オラクル | Wikidata↔OSM 直リンク 945 組(M-11)に対する再現率が閾値未満なら落とす。**マッチャーはこの ID を入力に使ってはならない**(使えば循環する) |
| G-10 | 本文の再配布禁止(D-01) | 公開アーティファクトに CC BY-SA 由来の本文・埋め込みベクトルが含まれていたら落とす |
| G-11 | 字種・制御文字 | `harness/text_hygiene.py` の違反があれば落とす |
| G-12 | ゲートとテストの対応 | どのテストからも ID 参照されず、SPEC に「未実装」とも書かれていない `G-xx` があれば落とす |
| G-13 | 史実と AI の非混在 | 神社詳細で「公開史料に基づく情報」と「AI による意味分析」が同一の見出しの下に出ていたら落とす |
| G-14 | DEM 復号 | 既知点(諏訪湖湖面 759.0 m ほか)の復号が許容差を超えたら落とす |
| G-15 | 出荷ビルド | `npm run build` が通らなければ落とす |

**未実装のゲート(V1.0 の後続ループで実装する)**: G-07 / G-13。

loop_004 の時点で実装済みなのは
G-01 / G-02 / G-03 / G-04 / G-05 / G-06 / G-08 / G-09 / G-10 / G-11 / G-12 / G-14 / G-15。
G-07(出典の欠落)は、由緒本文を持つレコードが 1 件も無い段階では**違反が無いときと
検査が働いていないときの区別が付かない**ので、コーパスを作るループで陽性対照とともに実装する。

## 7. 名寄せの非循環オラクル(この設計の背骨)

Wikidata の 945 件(M-11)は、**人間が「この Wikidata 項目はこの OSM 要素だ」と明示した対応**
である。我々のマッチャー(距離 + 名称類似度 + 補助特徴)は、この ID を**一切入力に使わない**。
したがって「945 組をどれだけ再現できたか」は循環しない。

仕様書 §8.3 は補助特徴に「OSM wikidata tag」を挙げているが、**D-02 としてこれを禁止する**。
`wikidata=*` タグと P10689/P11693/P402 は同じ対応関係の裏表であり、入力に混ぜるとオラクルが
恒等式になる(HC-045)。

### 7.1 オラクルの実測(2026-09-08)

| 測ったもの | 値 |
|---|---|
| 対象(OSM・東京都/京都府/山梨県) | 3,152 件 |
| Wikidata(日本・座標あり) | 16,908 件 |
| **オラクルのペア** | **825 組**(OSM タグ由来 744 / 両方向一致 79 / Wikidata 側 ID のみ 2、衝突 0) |
| 較正半分 / 取り置き半分 | 411 組 / 414 組(OSM ID の SHA-256 で決定的に分割) |

**較正半分だけを見て閾値を決め、取り置き半分で報告する。** 同じ標本で決めて同じ標本で
報告すると、何も分からない。

### 7.2 決定 D-03 — 判定閾値を仕様書 §8.4 から置き直す

仕様書 §8.4 の `>= 0.90 auto merge` は **D-02 のもとで到達不能**だった(HC-229)。
`external_id` の 0.10 は Wikidata↔OSM の相互リンクで埋まる前提だったが、それは
オラクルそのものなので入力に使えない。理想入力(距離 0 m・名称完全一致・行政区画一致・
別名一致)でも合計は 0.90 で頭打ち、実データの大半では 0.80 が上限になる。

較正半分の閾値掃き(2026-09-08、411 組):

| 閾値 | 自動結合 | 適合率 | 再現率 |
|---|---|---|---|
| 0.58 | 400 | 0.9975 | 0.9708 |
| **0.60** | **395** | **1.0000** | **0.9611** |
| 0.62 | 377 | 1.0000 | 0.9173 |
| 0.90(仕様値) | **0** | — | 0.0000 |

**採用: auto = 0.60 / review = 0.50。** 適合率が 1.0 になる最小の閾値を採った。
仕様書の重み(0.40/0.30/0.10/0.10/0.10)はそのまま使う。

### 7.3 取り置き半分での成績(標本外・2026-09-08)

| 指標 | 較正半分 | **取り置き半分** |
|---|---|---|
| top-1 一致 | 0.9927(408/411) | **0.9831**(407/414) |
| 自動結合の適合率 | 1.0000 | **0.9899**(393/397) |
| 自動結合の再現率 | 0.9611 | **0.9493** |

床は較正側の数字から余裕を見て置いた(再現率 0.90 / 適合率 0.95 / top-1 0.95)。
**取り置き半分を見て床を決めていない。**

## 7.4 系統分類の実測(2026-09-08)

結合できた属性(OSM 3,152 件のうち):

| 項目 | 件数 |
|---|---|
| 自動結合(スコア ≥ 0.60) | 955 |
| 要確認(0.50–0.59) | 109 |
| 祭神(P825) | 695 |
| 社格(P13723) | 461 |
| ja.wikipedia 記事 | 729 |
| 文献上の母院(P612) | 43 |
| 成立日(P571) | **2** |

**成立年代はほとんど取れない。** 全国の Wikidata でも P571 は 1,117 件しかなく、
3 都府県に落とすと 2 件になる。仕様書 §10 の時代スライダーは、この密度では成立しない。

### 二つの独立な信号の一致(循環しない照合)

祭神(P825)由来の系統と、名称規則由来の系統は互いを参照していない。

| 測ったもの | 値 |
|---|---|
| 両方が決まった件数 | 343 |
| 一致 | **340(99.13%)** |
| 食い違い | 3 |

**率だけを見ない。** 最初にこの照合を走らせたとき一致は 98.54% で、食い違い 5 件のうち
**2 件は分類器の欠陥**だった(祭神の答えが Wikidata の値の並び順で決まっていた)。
直した結果、一致率は一度 98.26% に**下がり**(比べる母数が増えたため)、
規則表の優先順位を整えて 99.13% になった。残る 3 件はいずれも実在の合祀である
(八坂と名乗る社に八幡神、稲荷と名乗る社に白山の神、天祖と諏訪の合社)。

食い違いは `data/reports/signal_disagreements.json` に残す。

### 地図の色について

系統は 17 種あるが、**色で塗り分けない**。地図は任意の二色が隣り合いうる形なので、
色覚多様性を含めて見分けのつく系列は 3 色までである。選んだ系統だけを強調色
`#b7410e` にし、残りを無彩色 `#8a8580` に落とす。この二色は CVD ΔE 12.6 /
通常視 ΔE 17.6 / 背景比 3:1 以上で分離する。

## 7.5 地理特徴量の実測(2026-09-08)

### 標高(F-06)

3,152 件すべてに付いた。DEM5A(z15)が 3,142 件、DEM10B(z14)が 10 件。
取得タイルは 1,651 枚(キャッシュ命中 1,531・不在 24)。

| 標高 m | 最小 | 25% | 中央 | 75% | 95% | 最大 |
|---|---|---|---|---|---|---|
| | −2.5 | 16.7 | 42.9 | 126.4 | 570.8 | **3,716.8** |

最大値は富士山頂の久須志神社。

**非循環オラクル: OSM の `ele` タグ。** OSM 投稿者が記録した標高であり、
DEM からの計算には一切使っていない。54 件を突き合わせた結果:

| 測ったもの | 値 |
|---|---|
| 差の中央値 | **0.14 m** |
| 1 m 以内 | 47 / 54 |
| 5 m 以内 | 52 / 54 |
| 10 m 以内 | 53 / 54 |
| 最大の差 | 59.56 m(名称タグの無い地物。`ele=600.0` という丸い値) |

### 最寄り河川距離(F-07)

出典は国土数値情報 W05。**CRS は `.prj` が無いのでメタデータ XML で確かめた**
(`JGD2000 / (B, L)` = EPSG:4612)。属性 `W05_004` が河川名であることも製品ページの
属性表で確認した。版は都道府県ごとに違う(東京都・山梨県 W05-08 / 京都府 W05-09)。

流路の本数: 東京都 1,388 / 京都府 5,151 / 山梨県 3,839。

| 距離 m | 最小 | 25% | 中央 | 75% | 95% | 最大 |
|---|---|---|---|---|---|---|
| | 0.6 | 133.6 | 293.7 | 579.1 | 1,546.9 | 6,641.0 |

**二経路一致(T-084)**: 都道府県ごとの平面直角座標系と UTM の二つで解き、
標本 180 件で**選ばれた河川・距離とも食い違い 0**。
許容は**相対**で置く(縮尺係数が距離に比例して効くため。実測の相対差は最大 1.27e-3)。

### 決定 D-04 — 島嶼部は「川が無い」と言う

**54 件で河川距離を `null` にした。** W05 には島嶼部の河川がほとんど無く、
上限を置かないと硫黄島の社に 221 km、八丈島の社に伊豆大島の三原川まで 66 km、
という値が付く。これは「近くに川がある」という主張として嘘である。

上限は **10,000 m**。2026-09-08 実測で、10 km 超は 54 件で**全部が島嶼部**、
本土で 10 km を超えるものは **0 件**、5〜10 km はわずか 3 件。この谷間に置いた。
落としたレコードには理由を残す(黙って `null` にしない)。

### 海岸距離(F-15)

**測らない。** `coast_distance_m` の欄は作り、値は `null` にする。
欄ごと無いと「測ったが 0 だった」と区別が付かない。

### 7.4 全国で測り直した(2026-09-12)

対象を 3 都府県 3,152 件から全国 40,792 件へ広げ、オラクルを取り直した。

| 測ったもの | 3 都府県(2026-09-08) | 全国(2026-09-12) |
|---|---|---|
| 対象(OSM) | 3,152 件 | **40,792 件** |
| オラクルのペア | 825 組 | **4,975 組**(OSM タグ 4,506 / 両方向 441 / Wikidata 側のみ 28) |
| 方向の衝突(不採用) | 0 組 | 3 組 |
| 較正半分 / 取り置き半分 | 411 / 414 | **2,517 / 2,458** |

**閾値の決め方が、標本を広げたら成り立たなくなった。** 3 都府県では「適合率が 1.0 になる
最小の閾値」として 0.60 を採った(§7.2)。全国の較正半分で同じ掃きをすると:

| 閾値 | 自動結合 | 適合率 | 再現率 |
|---|---|---|---|
| 0.50 | 2,472 | 0.9931 | 0.9754 |
| **0.60** | **2,409** | **0.9938** | **0.9511** |
| 0.70 | 2,043 | 0.9941 | 0.8069 |
| 0.72 | 209 | **1.0000** | 0.0830 |
| 0.90(仕様値) | 0 | — | 0.0000 |

**適合率はどの閾値でも 0.993〜0.994 で頭打ちになり、1.0 にはならない。**
0.72 で 1.0 に見えるのは、地理+名称+行政の上限 0.80 に当たって候補がほぼ消えるからで、
再現率は 0.083 に崩れる。**3 都府県で見えた「0.60 で適合率 1.0」は小標本の産物だった。**

閾値は **0.60 のまま据え置く**(較正半分で事前に決めた値であり、全国の較正でも膝の位置にある)。
決め方の根拠だけを「適合率 1.0 になる最小」から「**適合率がほぼ平らな帯の中で再現率が
落ち始める手前**」に書き直す。

**取り置き半分での成績(標本外・2026-09-12)**

| 測ったもの | 値 |
|---|---|
| top-1 一致 | **0.9874**(2,458 組) |
| 自動結合の適合率 | **0.9911**(2,361 組を自動結合) |
| 自動結合の再現率 | **0.9520** |
| review 帯(0.50〜0.59) | 61 組 |

G-09 の床(top-1 0.95 / 再現率 0.90 / 適合率 0.95)は **3 都府県の較正半分で決めた値のまま**
であり、全国の取り置きはそれを上回っている。床は取り置きを見て決めていない。

## 7.6 AI の実測(2026-09-08)

### コーパス

| 測ったもの | 値 |
|---|---|
| ja.wikipedia 記事を取得できた神社 | **729 / 729**(すべて版 ID つき) |
| 由緒に相当する節を抜き出した後のコーパス | **701**(120 字未満の 28 件を除外) |
| 由緒本文の長さ | 最小 124 / 中央 **347** / 上限 800 字(110 件を切り詰め) |
| 使った見出し | 導入 701 / 歴史 378 / 祭神 348 / 概要 205 / 由緒 105 ほか |

「交通アクセス」「参考文献」「外部リンク」等は除いてある。記事全体を入れると、
クラスタが**由緒ではなく記事の書き方**を測る。

### モデルとクラスタ

| 項目 | 値 |
|---|---|
| モデル | `intfloat/multilingual-e5-base` |
| 版(commit) | `d128750597153bb5987e10b1c3493a34e5a4502a` |
| 次元 | 768 |
| クラスタ | **12 個**(サイズ 15〜158)+ どれにも入らない **123 件** |
| 埋め込みの所要時間 | 2,650 秒(CPU・4 スレッド)。再利用時は 253 秒 |

**埋め込みは指紋(本文 + モデル + 版の SHA-256)で再利用する**(仕様書 §22)。
本文もモデルの版も変わっていないのに 44 分を再計算するのは浪費である。

### モチーフの陽性対照(T-096)

神社と無関係な文(料理の手順・決算の説明・アルゴリズムの解説)を同じ計算に通した。

| 測ったもの | 値 |
|---|---|
| 無関係な文の最高スコア | **0.8117** |
| 由緒本文の「最高スコア」の**最小値** | **0.8278** |
| 由緒本文の「最高スコア」の中央値 | 0.8544 |

**701 件すべてが、3 つの対照文すべてを上回った。** 対照は通っている。

### 決定 D-05 — 生のコサインを画面に出さない

仕様書 §12.4 の表示例(山岳 0.81 / 氏神 0.62 / 水 0.47)のような開きは、
このモデルでは**存在しない**。

| 測ったもの | 値 |
|---|---|
| 12 モチーフ × 701 件の全スコアの範囲 | **0.780 〜 0.882** |
| 1 神社の中の「最大 − 最小」 | 中央値 **0.042**(最小 0.019 / 最大 0.062) |

生の数値を 12 個並べると、読み手は「どれも同じくらい当てはまる」と受け取る。
そこで画面には**そのモチーフの分布に対する順位**(`motif_percentiles`)を出す。
生スコアは再現性のために JSON に残す。

**さらに悪いことが分かった。** 仕様書 §12.4 のようにスコア降順で並べると、
順位の中身は**神社の性質ではなくモチーフごとの基準値の差**になる。

| 測ったもの | 値 |
|---|---|
| 生スコアで最上位になったモチーフが「勧請・分祀」だった神社 | **463 / 701(66.0%)** |
| 生スコアで一度でも最上位になった「海・航海」 | **0 件** |
| 順位で最上位になったモチーフの最大シェア | 近代創建・合祀 **17.4%**(分布が均される) |
| 生スコアの最上位と順位の最上位が一致 | **202 / 701(28.8%)** |

つまり生スコアの表示は、**3 分の 2 の神社に同じ答えを返す**。
`/shrine` `/similar` `/ai-space` はすべて順位で並べる。

### T-097 — 由緒の文章と土地の条件は合っているか(循環しない照合)

モチーフのスコアは由緒の文章から、標高は国土地理院 DEM から、河川距離は
国土数値情報から出ている。互いを参照していないので、この照合は循環しない。

| 照合 | n | 順位相関 ρ | 並べ替え検定 p | 判定 |
|---|---|---|---|---|
| 山岳・自然 × 標高 | 701 | **+0.361** | **0.0005** | **成立** |
| 水・河川 × 河川距離 | 691 | −0.096 | 0.0105 | 向きは合うが有意でない |
| 海・航海 × 標高 | 701 | +0.052 | 0.185 | **期待と逆向き。成立しない** |

**三つのうち成り立ったのは一つだけである。** 当たらなかった二つも消さずに載せる。
海・航海が成り立たない理由は特定できていない。対象が東京都・京都府・山梨県で、
山梨は内陸、東京の島嶼部はむしろ標高の高い火山島であることが効いている可能性はあるが、
**確かめていない**。

## 7.7 公開(2026-09-09)

本番: **https://jinja-origin-atlas-ai.vercel.app**(素の名前が取れた)。
GitHub: https://github.com/twill3c/jinja-origin-atlas-ai。app-menu に掲載済み。

- 静的書き出し(`output: 'export'`)。**serverless function をひとつも作らない**
- 送る中身を先に測った: `data/raw` 199MB / `node_modules` 461MB / `out` 72MB。
  `.vercelignore` を書いて約 9MB に落としてから送った
- **ビルドの刻印**(`scripts/build_stamp.mjs` / `prebuild` で自動生成)。
  画面が読むデータの内容から作ったハッシュを配信物に置き、`harness/live.mjs` が
  冒頭で照合して、合わなければ他を一切見ずに止める。
  **本番検品は「健やかか」しか答えない。「新しいか」は別の仕掛けが要る**
- 刻印は**改行を LF に揃えてから**測る(この機は `core.autocrlf=true` で、
  生のバイト列だと同じ内容でも手元と本番で必ず食い違う)

初回デプロイ後の本番検品は 24 項目すべて通過(刻印 `0367ade3ae23561184b62f48a6ed008e`)。

## 7.8 決定 D-06 — 全国展開では神社ごとの HTML を作らない(2026-09-10・人間の判断)

3 都府県 3,152 件の成果物から全国 40,776 件(12.94 倍)を外挿した(2026-09-10):

| 測ったもの | 3 都府県(実測) | 全国(外挿) | 上限 |
|---|---|---|---|
| 静的ページ | 1,665 | 約 18,508 | — |
| 出荷ファイル | 3,360 | 約 43,478 | Vercel 無料枠のアップロード 5,000/24h |
| ビルド時間 | 約 10 分 | 約 111 分 | Vercel Hobby 45 分 |
| カタログ(単一 JSON) | 2.61 MB / gz 0.24 MB | 33.74 MB / gz 3.07 MB | gz 5 MB(G-06) |

**上限に二重に当たる。** 人間の判断で次の形を採った:

- 神社詳細 `/shrine/?id=<ID>&p=<県コード>` と類似 `/similar/?id=…` を**それぞれ 1 枚のクライアント描画ページ**にする
- 全カタログは配らない。**都道府県チャンク** `public/data/shrines/NN.json` と、ID → 県コードの
  **索引** `public/data/shrines/index.json` を配る(仕様書 §28.2 の形)
- AI のスコア(モチーフの順位・クラスタ・由来記事)は**チャンクに入れる** ——
  詳細を開くのに別のファイルを何本も引かせない
- **類似の相手だけは別ファイル**にする(`public/data/similar/NN.json`)。同居させると
  東京都のチャンクで欄の **52.6%**(1 件あたり 3,548 バイト)を占め、詳細を見るだけの人に
  要らないものを配ることになる(2026-09-11 実測)。分離で東京都チャンクは
  3.99 MB → **1.97 MB**(gz 0.198 MB)になった。相手の名前・県・系統・上位モチーフは
  その別ファイルに書き込んであるので、類似の画面も 2 本引けば描ける
- 県の帰属は**取得の経路**(どの県の Overpass 問い合わせで返ったか)から決める。
  `addr:province` は 3 都府県で 162/3,152 件にしか付いていなかった。県境の神社は二つの県に
  返るので、最初の県に帰属させて重複として数える(T-117)
- `?p=` が無い・外れているときは索引を引き直す。**外れた県コードを信じて「見つからない」と言わない**
- 旧 URL(`/shrine/<ID>/`)は 404 ページの中で新しい形へ送る(静的書き出しでは rewrite を置けない)

**実際にそうなったか(2026-09-12 実測・全国 40,792 件)**

| 測ったもの | D-06 前の見込み(外挿) | D-06 後の実測 |
|---|---|---|
| 静的ページ | 約 18,508 枚 | **10 枚** |
| 出荷ファイル | 約 43,478 個 | **103 個**(上限 5,000) |
| ビルド時間 | 約 111 分 | **約 1 分**(上限 45 分) |
| 配るカタログ | 単一 33.74 MB | 47 チャンク(最大 1.97 MB)+ 索引 1.01 MB + 類似 3 ファイル |

見込みは上限を二重に超えていたが、実測では**どちらの上限にも余裕をもって収まった**。
実ブラウザ検品 105 項目も通っている(3 県での詳細表示・県コード無しや外れた県コードからの
復帰・実在しない ID の陽性対照・旧 URL の転送・類似の相手のリンク形式)。

**仕様書からの逸脱を明記する。** 仕様書 §33 は「各神社詳細を静的生成できる範囲で metadata 生成」を
求めていた。D-06 で神社ごとの HTML をやめたので、検索エンジンに個別の頁として拾われなくなる。
タブの題名だけは画面が読み込み後に付け直す。

## 7.9 全国へ広げて出た外部データの壊れ方(2026-09-12)

3 都府県では一度も出ず、47 都道府県にして初めて出たものを残す。

| 何が | どこで | どうした |
|---|---|---|
| zip の中が**一階層深い** | 国土数値情報 W05 の **北海道(01)と島根(32)だけ** `W05-09_01-g_GML/` の下にある | 再帰で探す |
| **点が 1 個しかない線** | W05 **三重県(24)** の 7,527 本に 1 本 | 落とすが**数えて報告する**(`invalid_geometries_dropped`) |
| 接続の一時失敗 | Overpass に 2 回 | 指数バックオフで待ち直し、いずれも後続の試行で成功 |

**いちばん危なかったのは、壊れ方そのものではなく握りつぶしだった。**
河川の読み込みは `except FileNotFoundError: continue` で県を飛ばす作りになっており、
**北海道と島根の河川がまるごと欠けたまま通っていた**。下流から見ると
「その県の神社には近くに川が無かった」と区別が付かない。3 都府県では 3/3 が読めていたので、
この握りつぶしは一度も発火しておらず、**対象を広げた瞬間に 2 県を飲み込んだ**。
いまは読めない県があれば例外で止める(T-119 が 47 県の被覆を検査する)。

修正後の実測: **47 県 286,436 本**、落とした退化幾何 **1 本**。

**全国の最寄り河川距離(2026-09-12 実測)**

| 測ったもの | 値 |
|---|---|
| 距離が付いた神社 | **40,652 / 40,792**(99.66%) |
| 上限 10 km を超えて `null` にしたもの | **140 件**(3 都府県では 54/3,152 = 1.7%、全国では 0.34%) |
| 県コードの無い神社 | **0 件** |
| 距離の分布 | 最小 0.0 / 25% 114.2 / 中央 **242.0 m** / 75% 477.1 / 95% 1,181.4 / 最大 9,929.5 |
| 1 km 以内 | **37,735 / 40,652**(92.8%) |
| **二経路一致** | 940 標本で**食い違い 0** |

**全国の標高(2026-09-14 実測)**

| 測ったもの | 値 |
|---|---|
| 標高が付いた神社 | **40,787 / 40,792**(取れなかった 5 件は層別内訳に数えてある) |
| 使った層 | dem5a 39,802 / dem_png 568 / dem5b 225 / dem5c 192 |
| 分布 | 最小 -2.5 / 中央 **35.2 m** / 95% 468.6 / 最大 3,716.8 |
| **OSM の `ele` タグとの照合**(非循環) | **371 件**・差の中央 **0.31 m**・10 m 以内 362 件(97.6%) |

**取得にかかった時間は 46,106 秒で、見込み(約 80 分)の 8 倍だった。** ただし見込みが外れたのではない。
進捗行を区間ごとの速度に並べ直すと、取得は終始 7〜15 件/秒で、**20,000→25,000 件目の 1 区間だけが
42,233 秒(0.12 件/秒)止まっていた**。実働は約 63 分。停滞の原因は特定していない。
走っている間は進捗行が累計の経過秒しか出しておらず停滞に気づけなかったので、区間の速度を出すようにした(HC-264)。

照合の最大差は **926.6 m** だった。差の大きい 3 件を地理院の**標高 API**(タイル復号とは別の経路)に
当てると、3 件とも当方の復号と **0.1 m 以内で一致**した:

| 神社 | OSM `ele` | 当方のタイル復号 | 地理院 標高 API |
|---|---|---|---|
| 幸谷天神(千葉県) | 950.0 | 23.37 | 23.4(5m レーザ) |
| 金比羅神社(長崎県) | 0.0 | 178.44 | 178.4(1m レーザ) |
| 八坂神社(長崎県) | 0.0 | 36.80 | 36.9(1m レーザ) |

**外れていたのは DEM ではなく OSM のタグだった。** 千葉の低地に 950 m、長崎では同じ投稿の並びに
`ele=0.0` の埋め草が続く。**独立な出所の側も誤りを含む**ので、照合は率と中央値で床を置いている
(T-082)。1 件の最大差で落とす形にしていたら、正しい実装が落ちていた。

二経路一致は、県ごとの平面直角座標系と、**表を引かずに経度から決めた UTM 帯**で同じ問いを
解いている。表の割り当てを間違えていても(HC-262 で実際に II 系を 53N にしていた)、
この第二の経路は表を参照しないので影響を受けない。

## 8. スコープ外(V1.0)

- 御朱印の販売・在庫、参拝者レビュー、混雑予測、位置追跡、ユーザー投稿
- LLM API への常時接続、Vercel Functions 上の Transformer 推論
- 神社の「格付け」、AI による史実の断定
- 著作権状態が不明な現代由緒本文の大量収集・再配布
- ロボット排除規則・利用規約に反するスクレイピング
- 海岸距離(C23 の非商用条項のため V1.1 送り。F-15)
- 教師ありの BERT 多ラベル分類(仕様書 §12.5 の発動条件を満たさないため)
