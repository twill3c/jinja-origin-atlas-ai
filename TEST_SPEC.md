# TEST_SPEC.md — jinja-origin-atlas-ai

<!-- scaffold template v1.30.0 から展開(2026-09-08) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation` / `network`
- **`network` マークは既定で除外される**(`pyproject.toml` の `addopts = "-m 'not network'"`)。
  外部サービスに触れるケースは `pytest -m network` で明示的に走らせる。CI では走らせない
  (外部の可用性でビルドを落とさないため)。ただし**データ更新ワークフローでは走らせる**
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | 実測日と実測値をコメントに残す |

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| ジャパンサーチ `rights` ファセットの 18 コード | 開発者向け簡易 Web API ガイド 3.2.2.1 の `common.contentsRightsType` コード値表 + 実 API の facet(2026-09-08 実測) | **外部権威**。仕様書 §6.3 の ALLOW/DENY 集合はこの表を網羅していなかった |
| GSI DEM 復号の既知 RGB | 標高タイル仕様 https://maps.gsi.go.jp/development/demtile.html の式 | **仕様**。閉形式 |
| 諏訪湖湖面 759.0 m | 公表標高 759 m。`dem_png` z14 から復号して完全一致(2026-09-08 実測) | **外部権威 × 実測**。平坦面なので水平誤差に鈍い |
| Wikidata↔OSM 直リンク 945 組(SPEC M-11) | Wikidata の P10689 / P11693 / P402 | **非循環オラクル**。マッチャーはこの ID を入力に使わない(SPEC §7 / D-02) |

**富士山頂を DEM のオラクルに使わない。** 剣ヶ峰の座標で復号すると 3,756.03 m(dem5a z15)/
3,760.89 m(dem_png z14)で、公表 3,776 m と 15〜20 m ずれる(2026-09-08 実測)。
急斜面では水平方向のわずかなずれが標高差に化ける。**オラクルには平坦な既知点を選ぶ。**

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-05 / G-02 | `rights_gate`: `cc0` / `pdm` / `ccby` | `ai_usable=True` |
| T-002 | F-05 / G-02 | `rights_gate`: 実測 18 コードのうち ALLOW 以外すべて | `ai_usable=False` |
| T-003 | F-05 / G-02 | `rights_gate`: コード表に無い未知の文字列 | `ai_usable=False`(fail-closed) |
| T-004 | F-05 / G-02 | `rights_gate`: `None` / 空文字 / 空白のみ(実測で 33/100 が欠落) | `ai_usable=False` |
| T-005 | F-05 / G-02 | **網羅性**: 実測 18 コード全部が明示分類され、未分類に落ちない | 未分類 0 件 |
| T-006 | F-05 / G-02 | **陽性対照**: ALLOW 集合を空にすると T-001 が必ず落ちる | 検査が実際に効いている |
| T-007 | F-05 / G-02 | **陰性対照**: ALLOW を全コードに広げると T-002 が必ず落ちる | 検査が実際に効いている |
| T-008 | F-05 | `attribution_required` は `ccby` で真、`cc0` / `pdm` で偽 | 表示義務の判定 |
| T-009 | F-05 / G-02 | JPS レコード列を通すと ALLOW 以外が 1 件も残らない | 集合の一致(件数定数を書かない) |
| T-010 | F-06 / G-14 | DEM 復号: `x < 2^23` / `x == 2^23` / `x > 2^23` の 3 分岐 | 仕様の式どおり・`2^23` は `None` |
| T-011 | F-06 / G-14 | DEM 層の配信ズーム定数(`dem5a_png`=15 / `dem_png`=14) | SPEC M-17 の実測に一致 |
| T-012 | F-06 | 緯度経度 → タイル座標・タイル内画素。z=0 は常に (0,0)。既知点で往復 | 閉形式 |
| T-013 | F-06 / G-14 | **[network]** 諏訪湖湖面の標高を実タイルから復号 | 759.0 ± 0.5 m |
| T-014 | F-06 / G-14 | **陽性対照**: 復号式の係数を 1 つ壊した実装は T-010 を落とす | 検査が実際に効いている |
| T-020 | F-04 | 名称正規化: NFKC(全角英数・半角カナ)・空白畳み・前後トリム | 正規形が一致 |
| T-021 | F-04 | **陰性対照**: 「神社」等の接尾辞を削っただけで同一と判定しない | 八幡神社 と 八幡宮 は正規化だけでは一致しない |
| T-022 | F-04 | 名称類似度は対称で `[0,1]`、同一文字列で 1.0 | 不変量 |
| T-030 | F-01 / G-03 / G-04 | 生成 GeoJSON の全 Feature が日本の外接矩形内、`id` が一意 | 違反 0 |
| T-031 | F-01 / G-01 | 生成 GeoJSON が Feature/Point/座標 2 要素の構造を満たす | schema 準拠 |
| T-032 | F-02 | 出典文字列 `© OpenStreetMap contributors` が**出荷 HTML** に含まれる | JS 抜きで常時表示 |
| T-033 | N-01 / G-05 | 出荷物(`out/` の html / js / json 全部)に有料 API のホスト名が現れない | 違反 0 |
| T-034 | N-01 / G-15 | `npm run verify`(型検査 → vitest → `next build`)が通り、静的書き出しになる | serverless function 0 |
| T-035 | G-12 | SPEC のゲート表と本ケース表の対応を機械で数える。孤児ゲート 0 | 参照されるか未実装宣言があるか |
| T-040 | N-01 / G-11 | `harness/text_hygiene.py` が違反 0 | 字種・制御文字 |
| T-050 | F-03 | SPARQL 応答は HTTP 200 でも本文が JSON として解けなければ例外にする | 打ち切りを黙って通さない |
| T-051 | F-03 | `P31/P279*` の経路重複で同一 item が複数行になる。item で畳んで 1 レコードにする | 集合の一致 |
| T-052 | F-03 | 座標・別名など多値の項目は**決定的に**畳む(同じ入力から同じ出力) | 再実行で同一 |
| T-053 | F-03 | 日付の精度(年のみ / 年月 / 年月日)と紀元前を落とさずに年の範囲へ変換する | 伝承年代と混ぜない |
| T-060 | F-04 | 名寄せスコア = 0.40 地理 + 0.30 名称 + 0.10 行政 + 0.10 外部 ID + 0.10 別名(仕様書 §8.4) | 重みの合計 1.0・値域 [0,1] |
| T-061 | F-04 | 判定: ≥0.90 auto / 0.75–0.89 review / <0.75 separate(仕様書 §8.4) | 境界値ちょうどの帰属 |
| T-062 | F-04 / D-02 | **循環の禁止**: マッチャーに直リンク(`wikidata` タグ / P10689 等)を与えても与えなくても結果が変わらない | 同一の出力 |
| T-063 | F-04 / G-09 | **非循環オラクル**: 直リンクのペア集合に対する再現率。閾値は実測後に置く | 再現率 ≥ 実測に基づく床 |
| T-064 | F-04 | 陰性対照: 同名でも 100 km 離れていれば結合しない | separate |
| T-065 | F-04 / G-09 | **陽性対照**: 名称類似度を無視する実装にすると T-063 の再現率が下がる | 照合が実際に効いている |
| T-066 | F-04 / G-09 | オラクルのペア集合が空でないこと、かつ両方向(WD→OSM / OSM→WD)から作られていること | 走査対象が空でない |
| T-070 | F-08 | 系統分類の優先順位(構造化 → 祭神 → 名称 → 由緒 → AI)。上位が決めたら下位は上書きしない | `basis` が優先順位どおり |
| T-071 | F-08 | 分類できないものは `unknown` にし、**黙って `other` に落とさない** | 未分類が数えられる |
| T-072 | F-08 | 祭神が複数系統にまたがるとき(タケミカヅチ単独 vs 春日神と同座)は確信度を下げる | 曖昧さを潰さない |
| T-073 | F-08 | **二つの独立な信号の一致率を測る**: 祭神由来の系統と名称由来の系統。循環しない | 一致率を実測して記録 |
| T-074 | F-08 / G-03 / G-04 | 結合後のカタログで `id` 一意・座標が矩形内・`sources` が空でない | 違反 0 |
| T-080 | F-06 | DEM タイルはディスクにキャッシュし、同じタイルを二度取りに行かない | 取得回数が相異なるタイル数を超えない |
| T-081 | F-06 | 標高が付いた件数・付かなかった件数を数える。黙って `null` にしない | 内訳が出る |
| T-082 | F-06 / G-14 | **非循環オラクル**: OSM の `ele` タグと DEM 由来の標高を突き合わせる | 差の中央値・分布を実測 |
| T-083 | F-07 | 都道府県 → 平面直角座標系(東京=IX/6677・京都=VI/6674・山梨=VIII/6676) | 対応表 |
| T-084 | F-07 | **二経路一致**: 平面直角座標系と UTM の二つの投影で最寄り河川を求め、選ばれた河川と距離が一致する | 同じ河川・距離差 ≤ 1 m |
| T-085 | F-07 | **陽性対照**: 緯度経度のまま距離を測る実装は T-084 を落とす | 経路が違えば検出される |
| T-086 | F-07 | 河川距離は非負、最寄り河川名が空でない | 違反 0 |
| T-087 | F-06 / F-07 | 統計画面が読む build.json の欄に標高・河川の集計が入っている | 契約 |
| T-090 | F-09 / G-10 | **公開アーティファクトに本文も埋め込みベクトルも含まれない** | 違反 0 |
| T-091 | F-09 / G-08 | AI レコードにモデル名と revision hash がある | 欠けたら落とす |
| T-092 | F-09 | E5 の入力形式(`passage: ` / `query: `)を守る | 前置詞つき |
| T-093 | F-09 | 埋め込みは L2 正規化されている | ノルム 1 ± 1e-5 |
| T-094 | F-09 | 類似度は対称・自己を除外・Top-K = 20 | 不変量 |
| T-095 | F-09 | モチーフスコアは 12 種すべてに出て、値域が cosine の範囲に収まる | 12 種・[-1,1] |
| T-096 | F-09 | **陽性対照**: 神社と無関係な文は、どのモチーフでも由緒本文より低く出る | 検出が効いている |
| T-097 | F-09 | **非循環の照合**: 山岳・自然モチーフの高い神社は実際に標高が高いか(DEM は独立) | 実測して記録 |
| T-098 | F-09 | クラスタ番号に意味を与えない。番号は実行ごとに変わりうると明記する | 文書と UI |
| T-099 | F-09 / D-01 | 各神社に由来記事の題名・版 ID・ライセンスが付く | 帰属できないものは使わない |
| T-100 | N-02 / G-06 | 単一の公開 JSON が gzip 5 MB を超えない。**圧縮後で測る** | 違反 0 |
| T-110 | F-01 / G-03 | 都道府県チャンクの ID の和集合が全カタログと一致し、どの ID も 2 つのチャンクに入らない | 集合の一致 |
| T-111 | F-12 | 索引(ID → 県コード)が全 ID を覆い、指す先のチャンクに実際にその ID がある | 違反 0 |
| T-112 | F-01 | チャンク内の全レコードの県コードがチャンクの県コードと一致する | 違反 0 |
| T-113 | N-01 / D-06 | 出荷ファイル数が 5,000 未満(Vercel 無料枠のアップロード上限。記憶 vercel-deploy-quirks) | 上限未満 |
| T-114 | D-06 | 神社ごとの HTML を作らない(`out/shrine/` と `out/similar/` の HTML は 1 枚ずつ) | 回帰の防止 |
| T-115 | F-09 / F-12 | チャンクの AI 欄は `ai=true` の神社にだけあり、類似の相手はすべて実在する | 違反 0 |
| T-116 | F-01 | 県コード表: 47 件・01〜47・JIS X 0401 の名前。平面直角座標系の表と県名の集合が一致する | 表の整合 |
| T-117 | F-01 | 県をまたいで返った要素(境界の神社)は最初の県に帰属させ、重複として数える | 黙って捨てない |
| T-118 | F-12 / D-06 | **Python↔TypeScript の契約**: 索引・チャンク・類似の形が画面の読む型と合う(tests-js) | 欄が欠けたら落ちる |
| T-119 | F-07 | 河川の読み込みが **47 県すべて**を覆い、落とした退化幾何を数えている | 県が欠けたら落とす |
| T-120 | F-06 | 標高が全カタログを覆い、層別の内訳で取れなかった件数を数えている。**1 件も取れていない県が無い** | 地域の欠落を捕まえる |
| T-121 | F-09 / F-10 | **類似の相手に、自分と同じ ja.wikipedia 記事を持つ神社を出さない。** 一つのリストに同じ記事を二度出さない。陽性対照: 同記事を混ぜた偽の表で落ちる | 違反 0(loop_008 で 701 件中 71 件の 1 位が同記事だった) |
| T-122 | F-09 | 同じ記事を共有する神社は、モチーフ・順位・クラスタ・UMAP 座標がすべて同一(記事単位で一度だけ計算している) | 違反 0 |
| T-123 | F-09 / N-03 | 埋め込みの保存は**文書単位**: 1 件足しても既存の文書を作り直さない・途中で止めても保存済みの分から再開する・モデルの版が変われば作り直す(偽のモデルで数える) | 符号化の回数が一致 |
| T-124 | F-04 / D-08 | **名寄せは一対一**: 一つの Wikidata 項目に自動結合される神社は 1 社まで。単体: 同じ項目を二つの地物が争えば得点の高いほうが取り、負けたほうは空いている次の候補を取る | 違反 0(loop_009 で 222 項目 / 485 件が多対一だった) |
| T-125 | F-01 / D-08 | **同名統合の規則**(単体): 名前が同じで 150 m 以内なら 1 社。151 m なら別。両方が別の Wikidata 項目に結合されていれば統合しない。連鎖した成分に項目が二つ以上あれば成分ごと統合しない。残す地物は relation > way > node | 規則どおり |
| T-126 | F-01 / D-08 | 出荷カタログに、同名で 150 m 以内の組が**統合を見送った数と同じだけ**しか残っていない | 報告の数と一致 |
| T-127 | F-12 / D-08 | 統合で消えた ID は索引の `aliases` で実在の ID を指し、`ids` とは重ならない。旧 ID の URL は残った神社を開く(実ブラウザ) | 違反 0 |
| T-128 | F-01 / D-08 | 社の部分の語(本殿・拝殿・社務所・鳥居など)で終わる名前には印が付き、件数が報告に出る。統合はしない | 印の数が報告と一致 |
| T-129 | G-13 / F-12 | **史実と AI の非混在**(実ブラウザ): 神社詳細で、帯ごとに見出しがちょうど一つ・AI の内容(順位・クラスタ・モチーフ・AI 推定の根拠)は C 帯だけ・公開データの行(祭神・社格・標高など)は A 帯だけ。AI と祭神・社格を両方持つ神社で確かめる。**陽性対照 3 通り**(AI の項目を A 帯へ移す / C 帯の見出しを消す / 祭神の行を C 帯へ移す)がすべて検出される | 違反 0・対照 3/3 |
| T-130 | D-08 / F-12 | 社の部分の印(T-128)は、印のある神社の詳細にだけ注記として出る(実ブラウザ) | 出る / 出ない |
| T-131 | G-13 / F-02 | A 帯に出す系統の根拠が AI 由来(`ai` / `origin_text`)でない。分類器が AI の結果を根拠に使うようになったら、A 帯に AI の結果が出るので落とす | 違反 0 |
| T-097b | F-09 / D-07 | モチーフと地理条件の照合(T-097)は**記事単位**で数える。レポートに単位が記事と記録され、どの照合の n も記事数を超えない | 神社単位で数えていたら落ちる |
| T-132 | F-12 / RULE-09 | **画面の範囲の文が出荷物と合う**(実ブラウザ): ホーム・地図・統計に「全国 N 都道府県」(N はビルド報告の県の数)があり、旧範囲の語(東京都・京都府・山梨県 / 3 都府県 / 段階 1 / 旧い照合の結論)が無い。陽性対照と陰性対照つき | 違反 0(loop_011 で 3 画面に 5 ループ残っていた) |
| T-133 | D-09 / N-03 | **地理特徴量の再利用の計画**(単体): 前回の県チャンクで ID が同じで 1 m 以上動いていない神社は標高・河川距離を引き継ぐ。動いた・新しい・前回取れていなかった神社は測り直す。理由つきの空欄(島嶼部の河川)は理由ごと引き継ぐ | 規則どおり |
| T-134 | D-09 / F-07 / HC-263 | **増分の河川距離の被覆**: 報告に mode=incremental・再利用した件数・計算した件数を残し、計算に要る県の流路をすべて読んでいる(全件のときは T-119) | 欠けた県 0 |
| T-135 | D-09 | **月次 PR の本文**(単体): 神社・自動結合・AI・標高・河川距離の前後と差、件数の変わった県だけを並べる。変化が無ければそう書く | 表の行が一致 |
| T-136 | D-09 | **ワークフローの作法**: 手動起動がある・cron は毎時 0 分でない・時間の上限が 6 時間以内・PR を開き main へ直接 push しない・地理特徴量を再利用する | 違反 0 |
| T-137 | F-07 / G-11 | **河川名の符号化**: 河川距離の報告と出荷した県チャンクの河川名に Latin-1 補助の字(Shift_JIS を ISO-8859-1 で読んだ跡)が無い。W05 の読み込みは cp932 を明示し、化けた名前があれば読み込みで止める。陽性対照つき | 違反 0(loop_012 で北海道の 1,159 社が化けていた) |
| T-138 | F-03 / HC-228 | **core の組み立て**(単体): ラベルなしの core・神社のラベル・行政区画のラベルから、ラベルサービスと同じ行になる(ja → en → QID そのもの、行政区画の無い行には adminLabel を付けない) | 規則どおり(ラベルサービスつきの core は 2026-09-15 に決定的に切れるようになった) |
| T-139 | F-09 / HC-228 | **ja.wikipedia の 429**(単体): 429 のあと Retry-After(無ければ指数的に)待って取り直し、記事を返す。429 が続けば上限の回数で例外にする(空の記事にしない) | 規則どおり(月次の二回目で約 680 件目に 429 を踏んで止まった) |
| T-140 | F-03 / HC-228 | **切れた本文のキャッシュ**(単体): 切れた SPARQL 応答がエッジのキャッシュから返ったら、文面を変えて取り直す(同じ文面を二度投げない)。キャッシュを通らない応答が同じ長さで二度切れたときだけ重すぎると判定する | 規則どおり(2026-09-16、切れた本文が hit-local・age 62 で返り、文面を変えると全件返った) |
| T-141 | D-09 / G-11 | **モデルのキャッシュを作業ツリーの中に置かない**: ワークフローの HF_HOME がランナーの一時領域を指す。作業ツリーに置くと字種検査が tokenizer.json を走査して落ちる | 違反 0(五回目の実行で 37,084 件) |
| T-142 | F-03 / G-19 | **祭神の件数保存**: 公開する祭神アーティファクトの延べ言及数・社数・異なり数が、県チャンクの `deities` を数え直した値と一致する。定数では書かない | 三者が一致(違反 0) |
| T-143 | F-03 | **総本社オラクルの所在県**(凍結表の検証): `head_shrines.json` の 26 件について、総本社の Q-id から OSM カタログを引くか、Wikidata 座標の最寄り社を取るかして、主張した県と一致する | 不一致 0(2026-09-21 実測: 直接一致 12・最寄り一致 14・最大 371 m) |
| T-144 | F-03 / G-16 | **リフトは総本社を当てるか**(非循環オラクル): 祭神つき社の県分布を期待値として各県のリフトを出し、該当 3 社以上の県のうち最大の県が総本社の県と一致する数を数える。合格線 13/26 は実装前に人間が承認して凍結した。**測って落ちた(7/26・2026-09-21)。** 以後は値がレポートに載っていること、画面の文が `pass` に従うことを見る | 値がレポートにある。`pass` は false(7 < 13)。**リフトの計算に総本社の表を入力しない**(循環するため) |
| T-145 | F-03 / G-20 | **リフトの帰無分布**(陽性対照の裏返し): 祭神の割り当てを社のあいだで無作為に振り直すと、総本社の的中数が落ちる。振り直した 200 回の平均が実測を下回り、p < 0.05 | 実測 > 帰無の 95 パーセンタイル(2026-09-21 実測: 実測 7 / 帰無の平均 0.80・95 点 2・p = 0.0050) |
| T-146 | F-03 / G-17 | **同一視は共起の強さで見分けられるか**(測って落ちてよい): Jaccard 上位 8 件を同一視と予測したときの適合率を測り、レポートに残す。閾値 0.70 | 値がレポートにある。合否はどちらでもよいが、画面の文はレポートの `pass` に従う |
| T-147 | F-03 | **ペア分類の凍結表が実データと合う**: `deity_pair_labels.json` の 50 件が共起上位 50 と順位まで一致し、Q-id も県チャンクの値と一致する。型は 4 つの列挙のみ | 不一致 0 |
| T-148 | F-14 / G-18 | **配置座標の決定性**: 同じ入力から配置を二度計算するとビット一致する。出荷した `network.json` の座標とも一致する | 完全一致 |
| T-149 | F-14 | **畳める辺は同一視だけ**: ネットワークの辺の型が凍結表と一致し、`identity` 以外の辺は畳んでも消えない。陽性対照として、型を付け替えた入力では畳みが変わることを確かめる | 規則どおり |
| T-150 | F-03 | **未分類を推測で埋めない**: 共起 51 位以下の辺の型は `unlabeled` であり、画面にも「未分類」と出る | `unlabeled` 以外が無い |
| T-151 | F-14 / N-06 | **祭神画面の実ブラウザ検品**: `/deity` が開き、一柱を選ぶと三面(ネットワーク・地図・対応表)が同じ祭神に変わる。図の要素が viewBox に収まり、横溢れが無い(幅 390 / 1280 の二通り) | 違反 0 |
| T-152 | N-02 / G-06 | **祭神アーティファクトの容量**: `public/data/deity/` の各ファイルが gzip 5 MB を超えない | 超過 0 |

### まだ実装していないゲート(loop_004 時点)

SPEC §6 に「未実装」と明記したもの: **G-07**。

- **G-07(出典の欠落)** —— 由緒本文をこちらの公開物に持たない設計(D-01)になったため、
  「由緒本文を持つのに出典が空」というレコードは構造的に発生しない。**空振りする検査は
  違反が無いときと検査が壊れているときを区別できない**ので、当てる対象ができるまで書かない。
  代わりに T-099(AI レコードに由来記事の題名・版・ライセンスがある)が同じ趣旨を守っている
- **G-13(史実と AI の非混在)** —— loop_010 で実装した。見出しの下にあるものを DOM で数え(T-129)、陽性対照 3 通りで働くことを確かめている。系統の根拠が AI 由来でないことはデータ側でも見る(T-131)

### 検査のうち自動化していないもの

T-032 と T-034 は自動化してあるが、**画面が読めるかは目視でしか分からない**(HC-041)。
図・地図を触ったループの完了条件に「実ブラウザ検品(`node harness/smoke.mjs`)を通し、
スクリーンショットを目で見る」を含める。検品器は失敗を終了コードで知らせる。
